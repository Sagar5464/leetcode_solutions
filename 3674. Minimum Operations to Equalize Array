class Solution {
public:
    int minOperations(vector<int>& nums) {
        bool a=1;
        for(int i=0;i<nums.size()-1;i++){
            if(nums[i]!=nums[i+1]){
                a=0;
                break;
            }
        }
        if(a) return 0;
        else return 1;
    }
};
