/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* getConcatenation(int* nums, int n, int* r) {
    int* arr=(int *)malloc((2*n)*sizeof(int));
    for(int i=0;i<n;i++){
        arr[i]=nums[i];
        arr[i+n]=nums[i];
    }
    * r=2*n;
    return arr;
}
