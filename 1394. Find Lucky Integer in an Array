class Solution {
public:
    int findLucky(vector<int>& arr) {
        map<int,int>mpp;
        for(auto i:arr)mpp[i]++;
        int c=-1;
        for(auto i : mpp){
            if(i.second==i.first){
                c=i.first;
            }
        }
        return c;
    }
};
