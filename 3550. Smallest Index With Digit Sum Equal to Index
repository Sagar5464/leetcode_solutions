int digSum(int num){
    int sum = 0;
    while(num>0){
        sum += num%10;
        num/=10;
    }
    return sum;
}
int smallestIndex(int* nums, int n) {
    for(int i=0;i<n;i++){
    if(nums[i]==i && i<=9) return i;
    else{
        if(digSum(nums[i])==i) return i;
    }
}
    return -1;
}
