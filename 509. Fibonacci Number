int fib(int n){
    if(n==0)return 0;
    int t1 = 0, t2 = 1, nextTerm,i;
    nextTerm = t1 + t2;

    for ( i = 3; i <= n; i++) {
        printf("%d ", nextTerm);
        t1 = t2;
        t2 = nextTerm;
        nextTerm = t1 + t2;
    }
    return nextTerm;
}
