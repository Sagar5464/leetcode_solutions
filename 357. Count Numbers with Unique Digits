int countNumbersWithUniqueDigits(int n) {
    if (n == 0) {
        return 1;
    }
    int c = 10;
    int j = 9;
    for (int i = 2; i < n + 1; i++) {
        c += j * (11 - i);
        j *= 11 - i;
    }
    return c;
}
