int sumOfUnique(int* nums, int n) {
    int arr[101] = {0},sum=0;
    for(int i=0;i<n;i++)
    {
        arr[nums[i]]++;
    }
    for(int i=0;i<n;i++)
    {
        if(arr[nums[i]]==1) {
            sum+=nums[i];
        }
    }
    return sum;
}
