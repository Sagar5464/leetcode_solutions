int diagonalSum(int** mat, int m, int* matColSize) {
    int sum = 0;
    for (int i = 0; i < m; ++i){
        sum += mat[i][i];
        int j = m - 1 - i;
        if (i != j) sum += mat[i][j];
    }
    return sum;
}
