bool isAcronym(char** words, int wordsSize, char* s) {
    char *result=(char*)malloc((wordsSize+1)*sizeof(char));

    for(int i=0;i<wordsSize;i++){
        result[i]=words[i][0];
    }
    result[wordsSize]='\0';
    bool ans=(strcmp(result,s)==0);
    free(result);
    return ans;
}
