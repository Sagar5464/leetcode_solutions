int finalValueAfterOperations(char** operations, int n) {
    int X = 0;
    for (int i = 0; i < n; i++) {
        if (operations[i][1] == '+') {
            X++;
        } else {
            X--;
        }
    }
    return X;
}
