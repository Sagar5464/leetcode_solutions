int sumOddLengthSubarrays(int* arr, int n) {
     int c =0;
    for(int i=0;i<n;i++)
    {
        int s = 0;
        for(int j=i;j<n;j++)
        {
            s += arr[j];
            if((j-i)%2==0) c+=s;
        }
    }
    return c;
}
