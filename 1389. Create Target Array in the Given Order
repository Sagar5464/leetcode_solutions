class Solution {
public:
    vector<int> createTargetArray(vector<int>& nums, vector<int>& idx) {
        vector<int> arr;
        for (int i = 0; i < nums.size(); i++) {
            arr.insert(arr.begin() + idx[i], nums[i]);
        }
        return arr;
    }
};
