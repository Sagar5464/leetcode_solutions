/* Write your PL/SQL query statement below */
select 
    round(sum(temp)/count(distinct player_id), 2) as fraction
from 
(select
    player_id,
    case when event_date - min (event_date) over (partition by player_id) = 1 then 1 else 0 end as temp
  from 
    Activity
) A
