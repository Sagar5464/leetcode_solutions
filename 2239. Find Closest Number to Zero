int findClosestNumber(int* nums, int  n) {
    int c=INT_MAX;
    for(int i=0;i<n;i++){
        int a=nums[i];
        if(abs(a)<abs(c) || (abs(a)==abs(c) && a>c))
            c=a;
    }
    return c;
}
