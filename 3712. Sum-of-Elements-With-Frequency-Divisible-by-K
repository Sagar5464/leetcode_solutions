class Solution {
public:
    int sumDivisibleByK(vector<int>& nums, int k) {
        int c=0;
        map<int,int>arr;
        for(int i:nums)arr[i]++;
        for(auto i:arr){
            if(i.second%k==0){
                c+=i.first*i.second;
            }
        }
        return c;
    }
};
