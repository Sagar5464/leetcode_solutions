char* removeTrailingZeros(char* num) {
    int x=strlen(num);
    for(int i=x-1;i>=0;i--){
       if (num[i] != '0') {
            num[i + 1] = '\0';
            break;
        }
    }
    return num;
    
}
