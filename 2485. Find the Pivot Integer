int pivotInteger(int n) {
    int l = 0;
    int r = 0;
    for(int i = 1; i <= n; i++) {
        l = i * (i + 1) / 2;
        r = n * (n + 1) / 2 - i * (i - 1) / 2;
        if(l == r)
            return i; 
    }
    return -1; 
}
