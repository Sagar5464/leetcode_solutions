int countPairs(int* nums, int n, int k) {
    int c=0;
    for(int i=0;i<n;i++){
        for(int j=i+1;j<n;j++){
            if(nums[i]==nums[j] && (i*j)%k==0 && i<j){
                c++;
            }
        }
    }
    return c;
}
