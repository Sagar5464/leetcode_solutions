int maximumWealth(int** ac, int a, int* accountsColSize) {
    int max = 0;
    for (int i = 0; i < a; i++) {
        int k = 0;
        for (int j = 0; j < accountsColSize[i]; j++) {
            k += ac[i][j];
        }
        if (k > max) {
            max = k;
        }
    }
    return max;
}
