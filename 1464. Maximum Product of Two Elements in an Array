int maxProduct(int* nums, int n) {
    int a=0;
    for(int i=0;i<n;i++){
        for(int j=i+1;j<n;j++){
            if(a<(nums[i]-1)*(nums[j]-1)){
                a=(nums[i]-1)*(nums[j]-1);
            }
        }
    }
    return a;
}
