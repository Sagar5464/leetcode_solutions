class Solution {
public:
    int minLengthAfterRemovals(string s) {
        int c=0,cnt=0;
        for(auto i: s){
            if(i=='a'){
                c++;
            }
            else cnt++;
        }
        return abs(c-cnt);
    }
};
