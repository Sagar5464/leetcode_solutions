class Solution {
public:
    bool canJump(vector<int>& nums) {
         int j = 0, n = nums.size();
        for (int i = 0; i < n; i++) {
            if (i > j) return false;
            j = max(j, i + nums[i]);
            if (j >= n - 1) return true;
        }
        return true;
    }
};
