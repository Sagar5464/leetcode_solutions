class Solution {
public:
    int fillCups(vector<int>& a) {
        int s=0;
        int c =0;
        for(int i=0;i<3;i++){
            c+=a[i];
            s=max(s,a[i]);
        }
        return max(s,(c+1)/2);
    }
};
