class Solution {
public:
    string removeDigit(string n, char d) {
        string s=" ";
        for(int i=0;i<n.size();i++){
            if(n[i]==d){
                string t=n.substr(0,i)+n.substr(i+1,n.size());
                s=max(t,s);
            }
        }
        return s;
    }
};
