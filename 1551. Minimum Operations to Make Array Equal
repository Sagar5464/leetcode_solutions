int minOperations(int n) {
   int*arr=(int *)malloc(n*sizeof(int));
   int s=0,c=0;
   for(int i=0;i<n;i++){
    arr[i]=(2*i)+1;
   }
   for(int i=0;i<n/2;i++){
        if(arr[i]<arr[n-i-1]){
            c+=((arr[i]+arr[n-i-1])/2)-arr[i];
        }
   }
   return c;
}
