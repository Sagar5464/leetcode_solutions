int differenceOfSum(int* nums, int numsSize) {
    int s=0,c=0;
    for(int i=0;i<numsSize;i++){
        s+=nums[i];
        while(nums[i]>0){
            c+=(nums[i]%10);
            nums[i]=nums[i]/10;
        }
    }
    return abs(s-c);
}
